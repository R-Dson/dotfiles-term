---
description: Draft a commit message
argument-hint: "[change description]"
---
Draft a commit message for: $ARGUMENTS

If no description is provided, use the current staged changes.

Deliverable:
- Commit message only, plus a short verification note if needed.
